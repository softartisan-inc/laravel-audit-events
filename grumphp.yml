grumphp:
  stop_on_failure: true
  tasks:
    phpstan:
      configuration: phpstan.neon.dist
      ignore_patterns:
        - ^src/Mcp/
        - ^tests/Feature/
      memory_limit: "512M"
    pest: ~
